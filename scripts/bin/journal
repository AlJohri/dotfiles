#!/usr/bin/env bash
# Open today's Obsidian daily journal in nvim, creating it from the
# Daily Journal template if it doesn't exist yet.

set -e
journal_file=$(ensure-daily-journal)
nvim "$journal_file"
