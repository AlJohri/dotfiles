#!/usr/bin/env bash

set -euo pipefail

for dir in */; do
    if [[ -d "${dir}.git" ]]; then
        echo "Pulling ${dir%/}..."
        if git -C "$dir" pull; then
            echo "✓ ${dir%/} succeeded"
        else
            echo "✗ ${dir%/} failed"
        fi
    fi
done
